\documentclass{easytikz}

\begin{document}
  \tikzset{
    every node/.style={
      draw,
      circle,
      inner sep=0pt,
      minimum width=15pt
    },
    thick
  }
  \begin{tikzpicture}
    \node {a}
      child { node {b}
        child { node {d} }
        child { node {e} }
      }
      child { node {c}
        child { node {f} }
        child { node {g} }
      };
  \end{tikzpicture}
\end{document}